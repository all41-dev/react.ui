# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
build-output
coverage
*.local

.env

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.env
.env.development

# Local review notes
REVIEW_REPORT.md
claude_docs

# Local brand/theme overrides for the sandbox. This repo is public; brand palettes
# are not. Share these files with colleagues directly, never through git.
# See src/app/styles/README.md.
src/app/styles/*.local.css

# Articles demo — product data, kept out of the library repo.
# NOTE: src/app/App.tsx imports this folder, so a fresh clone will not build until
# either the folder is restored or that import and its tab are removed.
# src/app/demos/articles/
